[package]
name = "maguey"
version = "0.1.0"
authors = ["Ricardo Delfin <me@rdelfin.com>"]
edition = "2018"

[dependencies]
sdl2 = { version = "0.32.2", features = ["bundled", "static-link"] }
gl = { path = "lib/gl" }

[features]
gl_debug = ["gl/debug"]
